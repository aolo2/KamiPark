package framework;

/**
 * Created by aolo2 on 5/22/15.
 */

public enum ObjectId {
    Player(), Air(), GhostBlock(), Spike(), Popup(), Piston(), BackGround(), Block(), SquareSaw(), GraphicalText();
}
